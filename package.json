{
  "name": "angular-app",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn workspaces run build"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    
  },
    "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.18.1",
    "yarn": ">=1.22.19"
  }
}